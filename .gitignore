# Bazel
/bazel-*
/.bazelrc.user

# Node (the hermetic build never writes this; only a stray `npm install` does)
node_modules/

# Output of running tools/build-workouts.mjs outside Bazel
dist/

# IDEs and OS
*.iml
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
local.properties
